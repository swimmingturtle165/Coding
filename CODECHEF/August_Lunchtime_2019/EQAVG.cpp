#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    long t;
    cin>>t;
    while(t--)
    {
        long n,k;
        cin>>n>>k;
        vector<int> inp,dis;
        map<int,vector<int>> m;
        
        for(int i = 0 ; i <n;i++)
        {
            cin>>temp;
        }
    }

}