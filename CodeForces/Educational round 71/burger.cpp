#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    long t;
    cin>>t;
    while(t--)
    {
        long b,f,p,h,c,ans=0;
        cin>>b>>f>>p>>h>>c;
        if(h>c)
        {
           if(p>=b*2)
           {
               ans+=h*p;
               b-=p*2;
           }
           else
           {
               long pos=
               ans+=
           }
           
        }
        else
        {
            
        }
        
    }
}